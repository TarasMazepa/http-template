{
  "name": "httpt-js-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test 'packages/*/test/**/*.js' '../test-fixtures/verify.js'",
    "lint": "npm run lint --workspaces"
  }
}
